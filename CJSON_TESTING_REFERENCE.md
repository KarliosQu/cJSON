# cJSON 测试规范与预期输入输出分析

## 测试数量总览（置顶）

按 `CTest` 注册口径：

- `test.c`：`1`
- `tests/`（核心）：`18`
- `tests/`（启用 `ENABLE_CJSON_UTILS`）：`21`
- 总计（不启用 utils）：`19`
- 总计（启用 utils）：`22`

按 `RUN_TEST(...)` 用例口径：

- `tests/*.c` 源码统计：`162`

按本文档定义测试项口径：

- 第一章 `test.c` 测试项：`8`（`T-01` 到 `T-08`）

## 第一章：`test.c` 测试内容（示例与冒烟路径）

本章仅描述 `test.c` 中体现的测试方式与预期输入输出，用于复现 cJSON 的示例级测试路径。

### 1.1 测试目标

`test.c` 主要覆盖：

1. 通过 API 构造 JSON 数据结构
2. 调用 `cJSON_Print` 与 `cJSON_PrintPreallocated`
3. 验证预分配缓冲区在“足够/不足”时的行为
4. 覆盖部分数值边界路径（包含无穷大相关输入路径）

### 1.2 构造与打印测试（输入与预期）

| 编号 | 输入/操作 | 预期 |
| --- | --- | --- |
| T-01 | 构造 Video 对象：含 `name`、`format.type/width/height/interlace/frame rate` | `cJSON_Print` 成功，输出为合法 JSON 文本 |
| T-02 | 构造字符串数组：`[Sunday..Saturday]` | 打印成功，数组顺序保持一致 |
| T-03 | 构造二维整型矩阵数组 | 打印成功，嵌套数组结构正确 |
| T-04 | 构造 Image 对象（含 Thumbnail、IDs） | 打印成功，对象层级与字段存在 |
| T-05 | 构造 records 数组（对象数组） | 打印成功，每个对象字段完整 |

### 1.3 `PrintPreallocated` 成功/失败路径

| 编号 | 输入/操作 | 预期 |
| --- | --- | --- |
| T-06 | 对同一 JSON，分配 `strlen(cJSON_Print)+5` 大小缓冲区并调用 `cJSON_PrintPreallocated` | 返回成功（true），输出 JSON 可读 |
| T-07 | 对同一 JSON，分配 `strlen(cJSON_Print)` 大小缓冲区并调用 `cJSON_PrintPreallocated` | 返回失败（false），体现“缓冲区不足” |

### 1.4 数值边界路径

| 编号 | 输入/操作 | 预期 |
| --- | --- | --- |
| T-08 | 向对象写入 `number = 1.0 / 0.0` 再打印 | 不崩溃；打印行为与解析器实现约定一致（通常应避免输出非法 JSON 数字） |

### 1.5 其他解析器复现建议（`test.c` 章节）

1. 按同样数据结构构造顺序创建 JSON。
2. 同时测试普通打印与预分配打印。
3. 必须包含一个“缓冲区不足”失败用例。
4. 对极端数值输入至少保证不崩溃、不越界。

## 第二章：`tests/` 测试内容（完整功能与边界规范）

本章为主要对标章节，定义应在其他 JSON 解析器中实现的核心测试集合。

### 2.1 解析测试规范（输入与预期）

#### 2.1.1 值类型解析

| 编号 | 输入 | 预期 |
| --- | --- | --- |
| P-V-01 | `null` | 解析成功，类型为 null |
| P-V-02 | `true` | 解析成功，类型为 true |
| P-V-03 | `false` | 解析成功，类型为 false |
| P-V-04 | `1.5` | 解析成功，类型为 number |
| P-V-05 | `"hello"` | 解析成功，类型为 string |
| P-V-06 | `[]` | 解析成功，类型为 array |
| P-V-07 | `{}` | 解析成功，类型为 object |

参考：`tests/parse_value.c`

#### 2.1.2 数字解析

| 编号 | 输入 | 预期 |
| --- | --- | --- |
| P-N-01 | `0` | 成功，数值为 0 |
| P-N-02 | `-2147483648` | 成功，保留负整数语义 |
| P-N-03 | `2147483647` | 成功，保留正整数语义 |
| P-N-04 | `10e-10` | 成功，数值约等于 `1e-9` |
| P-N-05 | `123e+127` | 成功 |
| P-N-06 | `9999999999999999999999999999999999999999999999912345678901234567` | 成功（大数可解析） |
| P-N-07 | `99999999999.1234567890.1234567` | 失败（非法双小数点） |
| P-N-08 | `99999E1234567890e1234567` | 失败（非法指数链） |

参考：`tests/parse_number.c`、`tests/misc_tests.c`

#### 2.1.3 字符串与 Unicode 解析

| 编号 | 输入 | 预期 |
| --- | --- | --- |
| P-S-01 | `""` | 成功，空字符串 |
| P-S-02 | `"\\\"\\\\\\/\\b\\f\\n\\r\\t\\u20AC"` | 成功，正确解码转义与 `€` |
| P-S-03 | `"\\uD83D\\uDC31"` | 成功，解码为 `🐱` |
| P-S-04 | `Abcdef\\e23` | 失败（非法转义） |
| P-S-05 | `"000000000000000000\\` | 失败（末尾反斜杠，防溢出） |

参考：`tests/parse_string.c`

#### 2.1.4 数组与对象解析

| 编号 | 输入 | 预期 |
| --- | --- | --- |
| P-A-01 | `[]` | 成功，空数组 |
| P-A-02 | `[1,null,true,false,[],"hello",{}]` | 成功，元素类型顺序正确 |
| P-A-03 | `{"one":1,"two":2,"three":3}` | 成功，键值可正确取出 |
| P-A-04 | `{"one":1,"NULL":null,"TRUE":true}` | 成功，多类型字段正确 |
| P-A-05 | `{"hello":[]}`（以 array parser 测） | 失败（非数组输入） |
| P-A-06 | `["hello",{}]`（以 object parser 测） | 失败（非对象输入） |

参考：`tests/parse_array.c`、`tests/parse_object.c`

#### 2.1.5 定长输入与解析结束位置

| 编号 | 输入 | 预期 |
| --- | --- | --- |
| P-L-01 | 有效 JSON 缓冲区 + 精确长度（无 `\0`） | 成功 |
| P-L-02 | 同一缓冲区 + 截断长度 | 失败，且不得越界读取 |
| P-L-03 | `[] empty array XD` | 成功，`parse_end` 指向 `[]` 之后 |
| P-L-04 | `{}x` 且要求完整终止 | 失败 |
| P-L-05 | `\xEF\xBB\xBF{}` | 成功（支持 UTF-8 BOM） |

参考：`tests/parse_examples.c`、`tests/parse_with_opts.c`

### 2.2 打印测试规范（输入与预期）

#### 2.2.1 值打印

| 编号 | 输入值 | 预期输出 |
| --- | --- | --- |
| PR-V-01 | null | `null` |
| PR-V-02 | true | `true` |
| PR-V-03 | false | `false` |
| PR-V-04 | 1.5 | `1.5` |
| PR-V-05 | `"hello"` | `"hello"` |
| PR-V-06 | [] | `[]` |
| PR-V-07 | {} | `{}` |

参考：`tests/print_value.c`

#### 2.2.2 数字打印

| 编号 | 输入 | 预期输出 |
| --- | --- | --- |
| PR-N-01 | `0` | `0` |
| PR-N-02 | `-32768` | `-32768` |
| PR-N-03 | `2147483647` | `2147483647` |
| PR-N-04 | `10e-10` | `1e-09` |
| PR-N-05 | `123e+127` | `1.23e+129` |
| PR-N-06 | `-123e-128` | `-1.23e-126` |

参考：`tests/print_number.c`

#### 2.2.3 字符串打印

| 编号 | 输入 | 预期输出 |
| --- | --- | --- |
| PR-S-01 | 空字符串 | `""` |
| PR-S-02 | 控制字符混合 | 控制字符按 JSON 规则转义 |
| PR-S-03 | `ü猫慕` | 输出 UTF-8 字符串（必要字符正确转义） |

参考：`tests/print_string.c`

#### 2.2.4 数组与对象打印（格式化/非格式化）

| 编号 | 输入 | 预期输出 |
| --- | --- | --- |
| PR-A-01 | `[1,2,3]`（非格式化） | `[1,2,3]` |
| PR-A-02 | `[1,2,3]`（格式化） | `[1, 2, 3]` |
| PR-O-01 | `{"one":1,"two":2}`（非格式化） | `{"one":1,"two":2}` |
| PR-O-02 | `{"one":1,"two":2}`（格式化） | `{
	"one":	1,
	"two":	2
}` |

参考：`tests/print_array.c`、`tests/print_object.c`

### 2.3 构造与 Add API 测试规范

| 编号 | 操作 | 预期 |
| --- | --- | --- |
| A-01 | `AddNullToObject(root,"null")` | 成功，字段类型为 null |
| A-02 | `AddTrueToObject(root,"true")` | 成功，字段类型为 true |
| A-03 | `AddFalseToObject(root,"false")` | 成功，字段类型为 false |
| A-04 | `AddBoolToObject(root,"b",false)` | 成功，字段类型为 false |
| A-05 | `AddNumberToObject(root,"n",42)` | 成功，值为 42 |
| A-06 | `AddStringToObject(root,"s","Hello World!")` | 成功，字符串一致 |
| A-07 | `AddRawToObject(root,"raw","{}")` | 成功，类型为 raw |
| A-08 | `AddObjectToObject(root,"obj")` | 成功，子对象存在 |
| A-09 | `AddArrayToObject(root,"arr")` | 成功，子数组存在 |
| A-10 | 任一 Add API 传入 `NULL` | 失败，返回空或 false，不崩溃 |
| A-11 | 注入分配失败后执行 Add API | 失败，返回空或 false，不泄漏 |

参考：`tests/cjson_add.c`

### 2.4 比较与最小化测试规范

#### 2.4.1 比较（Compare）

| 编号 | 输入 A | 输入 B | 模式 | 预期 |
| --- | --- | --- | --- | --- |
| C-01 | `1` | `1` | 任意 | 相等 |
| C-02 | `1` | `2` | 任意 | 不相等 |
| C-03 | `{"false":false}` | `{"False":false}` | 大小写敏感 | 不相等 |
| C-04 | `{"false":false}` | `{"False":false}` | 大小写不敏感 | 相等 |
| C-05 | `[1,2,3]` | `[1,2]` | 任意 | 不相等 |
| C-06 | `{"one":1,"two":2}` | `{"one":1,"two":2,"three":3}` | 任意 | 不相等 |

参考：`tests/compare_tests.c`

#### 2.4.2 最小化（Minify）

| 编号 | 输入 | 预期输出 |
| --- | --- | --- |
| M-01 | `{ "key":\ttrue\r\n }` | `{"key":true}` |
| M-02 | `{// comment\n}` | `{}` |
| M-03 | `{/* a\ncomment */}` | `{}` |
| M-04 | `"this is a string \\\" \\t bla"` | 字符串内容保持不变 |
| M-05 | 特殊输入 `8 / 5\n` | 不得出现死循环 |

参考：`tests/minify_tests.c`

### 2.5 鲁棒性与边界测试规范

| 编号 | 测试点 | 输入/操作 | 预期 |
| --- | --- | --- | --- |
| R-01 | 空指针防御 | 对公开 API 传入 `NULL` | 返回失败，不崩溃 |
| R-02 | 深度限制 | 构造超过嵌套上限的 JSON | 解析失败 |
| R-03 | 循环引用复制 | 构造循环结构后深拷贝 | 复制失败但不崩溃 |
| R-04 | SetValuestring 重叠 | 用重叠内存更新字符串 | 返回失败，避免未定义行为 |
| R-05 | realloc 失败路径 | 注入失败 realloc | 操作失败且不崩溃 |
| R-06 | 大数合法/非法边界 | 合法超大数与畸形大数 | 合法通过，畸形拒绝 |
| R-07 | 数组链表完整性 | 删除数组元素后打印 | 链表结构不损坏 |

参考：`tests/misc_tests.c`

### 2.6 cJSON_Utils（可选）测试规范

#### 2.6.1 JSON Pointer

| 编号 | 文档 | Pointer | 预期 |
| --- | --- | --- | --- |
| U-PTR-01 | `{"foo":["bar"]}` | `/foo/0` | 命中 `"bar"` |
| U-PTR-02 | `{"a/b":1}` | `/a~1b` | 命中 `1` |
| U-PTR-03 | `{"m~n":8}` | `/m~0n` | 命中 `8` |

参考：`tests/old_utils_tests.c`

#### 2.6.2 JSON Patch

| 编号 | 输入文档 | Patch | 预期 |
| --- | --- | --- | --- |
| U-PATCH-01 | 语料集样例 | 有效 patch | 应用成功，结果与 expected 一致 |
| U-PATCH-02 | 语料集样例 | 非法 patch | 应用失败 |
| U-PATCH-03 | doc 与 expected | 自动生成 patch 后回放 | 回放结果应等于 expected |

参考：`tests/json_patch_tests.c`、`tests/json-patch-tests/*.json`

#### 2.6.3 Merge Patch

| 编号 | 原文档 | 合并补丁 | 预期结果 |
| --- | --- | --- | --- |
| U-M-01 | `{"a":"b"}` | `{"a":"c"}` | `{"a":"c"}` |
| U-M-02 | `{"a":"b"}` | `{"a":null}` | `{}` |
| U-M-03 | `[1,2]` | `{"a":"b","c":null}` | `{"a":"b"}` |

参考：`tests/old_utils_tests.c`

### 2.7 其他解析器执行规范

1. 同一输入在严格模式下的成功/失败判定应与本章一致。
2. 对成功用例至少校验：
   - 解析成功
   - 关键字段值/类型一致
   - 序列化后可再次解析
3. 对失败用例至少校验：
   - 返回失败
   - 不崩溃
   - 不越界读取（建议配合 ASan/Valgrind）
4. 对格式化输出执行字符串全量比对。
5. 浮点打印允许极小平台差异，但需保证语义等价（可二次解析比较数值）。

### 2.8 最小可执行基线集

建议先跑 20 条：

- 解析基线：`P-V-01..07`（7 条）
- 数字基线：`P-N-01..05`（5 条）
- 字符串基线：`P-S-01..03`（3 条）
- 边界基线：`P-L-01..03`（3 条）
- 最小化基线：`M-01`、`M-02`（2 条）

通过后再扩展至全量与语料集测试。
